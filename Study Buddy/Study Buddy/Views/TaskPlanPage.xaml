<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Study_Buddy.Views.TaskPlanPage"
             Title="Task Planning"
             xmlns:local="clr-namespace:Study_Buddy.ViewModels"  
             xmlns:vm="clr-namespace:Study_Buddy.ViewModels" 
             xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker">
    <!--
      x:DataType enables compiled bindings for better performance and compile time validation of binding expressions.
      https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/data-binding/compiled-bindings
    -->
    <ContentPage.BindingContext>
        <vm:TaskPlanViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="3" Padding="15">
                <Label Text="Task Name" FontSize="Medium" />
                <Entry Text="{Binding Text, Mode=TwoWay}" FontSize="Medium" />

                <!-- Group or not Group work -->
                <StackLayout Orientation="Horizontal">
                    <Switch />
                    <Label Text="Group Task" FontSize="Medium"/>
                </StackLayout>
                <!-- Dated-->
                <StackLayout Orientation="Horizontal">
                    <Label Text="Due Date" FontSize="Medium" Padding="10,0"/>
                    <DatePicker/>
                </StackLayout>
                <!-- Time-->
                <StackLayout Orientation="Horizontal">
                    <Label Text="Time" FontSize="Medium" Padding="10,0"/>
                    <TimePicker/>
                </StackLayout>
                <Label Text="Description" FontSize="Medium" />
                <StackLayout BackgroundColor="LightGray" Padding="1" >
                    <Editor Text="{Binding Description, Mode=TwoWay}" AutoSize="TextChanges" FontSize="Medium" Margin="0" BackgroundColor="White"/>
                </StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Entry Text="ExampleText" FontSize="Medium" />
                    <Button Text="Colour Picker" Command="{Binding ColourPickerCommand}" HorizontalOptions="FillAndExpand"/>
                </StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                    <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
